=== 当前任务：Step 2 — 岗位归一化与岗位族构建 ===

⚠️ 重要：收到步骤开始指令后，直接调用 execute_python 执行分析，不要先回复"好的"或复述步骤说明。

目标：将碎片化的原始职位名称归一化为标准岗位，并构建岗位族体系。

内置分析函数说明：
系统已预注入以下函数，可直接调用（无需自己编写）：
- `_step2_normalize(df, col_map=None, scheme_index=0)` — 完整流程：行业推断 + 方案推荐 + 岗位归一化 + 薪酬验证。自动打印格式化结果。
- `_load_cached('step1')` — 加载 Step 1 缓存结果（col_map 等）

执行步骤（尽量合并执行，减少工具调用次数）：

第一步：执行归一化（一次 execute_python 调用）
  调用 execute_python：
  ```python
  result = _step2_normalize(_df)
  ```
  函数自动完成：
  - 从部门/职位关键词推断行业类型
  - 推荐 2-3 个岗位族方案
  - 归一化所有职位名称并分配到岗位族
  - 用薪酬重叠度验证分组合理性
  - 为 _df 添加 standard_position 和 job_family 列
  - 打印完整格式化结果

  向用户展示结果并请求确认。

第二步：如用户需要切换方案
  调用 execute_python：
  ```python
  result = _step2_normalize(_df, scheme_index=1)  # 切换到方案2
  ```

第三步：保存并汇总
  ⚠️ 必须调用 save_analysis_note：
  key: "step2_summary"
  content: 必须包括：
  · 行业推断结论（行业类型、企业规模、依据）
  · 选定的岗位族方案（岗位族数量和名称列表）
  · 低置信度归类的最终决定（用户确认后的结果）
  · 各岗位族人数分布统计
  调用 update_progress 标记步骤完成

确认卡点：
"帮你过一下岗位归一化的结果：
1. 岗位族方案合适吗？（可以自由调整，比如'把品质合并到生产里'）
2. 各岗位的标准名称和归属有没有不对的？
3. 低置信度的归类麻烦你逐个看一下。

没问题的话我就进入第三步：职级推断与定级。"

## 计划维护
步骤开始时调用一次 `update_plan` 列出待办项，步骤完成时再调用一次更新为已完成状态。不要每个子任务都单独调用。格式：
- [x] 已完成的任务及结论
- [ ] 待完成的任务
- 关键发现（简要记录）
