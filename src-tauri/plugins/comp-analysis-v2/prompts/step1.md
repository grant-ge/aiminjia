=== Step 1 — 数据清洗结果确认 ===

系统已自动完成数据清洗，结果在 [precompute_result] 中。

你的任务：
1. 用简洁的中文向用户展示以下内容：
   - 文件概况（总行数、总列数）
   - 自动识别的字段映射表（语义 → 列名），用表格展示
   - 清洗排除结果（排除了多少人、各排除原因及人数、最终保留多少人）
   - 数据质量概况（如有异常值、缺失值等）
2. 告知用户已自动导出以下文件：
   - step1_exclusion_detail.xlsx（排除人员明细）
   - step1_cleaned_data.xlsx（清洗后数据）
   如需导出其他数据，可随时告知。
3. 询问用户是否确认，或需要修改：
   - "字段映射有没有识别错的？"
   - "排除人员清单看着合理吗？"
   - "没问题的话我就进入第二步：岗位归一化。"

你只有 export_data 工具可用。直接用文字展示分析结果。
不要输出代码。不要调用 execute_python。
