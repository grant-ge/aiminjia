# Step 2: 参与度与留存关联

系统已自动完成功能使用分析，结果在 [precompute_result] 中。

**如果 [precompute_result] 有 `feature_ranking`：**
- 展示功能使用频率排名
- 展示功能渗透率（各功能的用户覆盖率）
- 展示常见用户路径（Top paths）

**降级情况：**
- 使用 `execute_python` 手动计算

1. **参与度评分** — 综合活跃天数、使用时长、功能深度
2. **高参与度用户特征** — 他们有什么共同点
3. **参与度与留存关联** — 参与度高的用户留存是否更好
4. **激活关键行为** — 哪些行为与长期留存强相关

展示结果后等待用户确认。
