# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust build artifacts
src-tauri/target

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# Tauri generated
src-tauri/gen/

# Claude Code
# 注意：不能直接 `.claude/` + `!.claude/skills/`——git 不允许在父目录被屏蔽时
# 用 `!` 重新包含子项。改为显式列出要屏蔽的具体子项，让 .claude/skills/ 自然入仓。
.claude/settings.local.json
.claude/memory/
.claude/worktrees/
.claire/worktrees/

# Local tool scratch dirs
.codex/
.codegraph/
.superpowers/
.worktrees/

# Playwright local artifacts (logs, snapshots, screenshots)
.playwright-cli/
.playwright-mcp/
output/

# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.pyc

# Plan-A: visual realignment captures
tmp/ui-capture/

dist-skills/

# Release
.release-state.json
signing-workspace/
.env.local.aijia

# Bundled runtime artifacts (built by scripts/prepare-bundled-runtime.{sh,ps1})
src-tauri/resources/runtime/
.runtime-cache/

# Build outputs / artifacts
build/
build-logs/

# LongMemEval 评测数据集（大文件，不进版本控制；用 tools/longmemeval/download.ps1 下载）
tools/longmemeval/data/
# 评测逐条明细产出
lme_eval_details.jsonl
